{
  "name": "bdsmlr-client",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "export VITE_BUILD_ENV=$(grep 'export const ACTIVE_ENV' src/config.ts | cut -d\"'\" -f2) && export VITE_BUILD_SHA=${VITE_BUILD_ENV}@$(git rev-parse --short HEAD) && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@lit-labs/router": "^0.1.4",
    "chroma-js": "^3.2.0",
    "date-fns": "^4.1.0",
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@types/chroma-js": "^3.1.2",
    "typescript": "^5.3.0", "vitest": "^2.1.0",
    "vite": "^5.0.0"
  }
}
