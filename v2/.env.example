# =============================================================================
# API Configuration
# =============================================================================
# For local development with Vite proxy (recommended):
VITE_API_BASE_URL=/api
# For production builds or direct API access:
# VITE_API_BASE_URL=https://api-staging.bdsmlr.com/api
#
# Note: The Vite proxy forwards /api/* to https://api-staging.bdsmlr.com/api/*
# This avoids CORS issues during development.

VITE_AUTH_EMAIL=user@example.com
VITE_AUTH_PASSWORD=secret

# =============================================================================
# Client Routing Blueprint (routes.py)
# =============================================================================
# Routing mode: 'path' for staging, 'subdomain' for production
# - path: /blogname/page/ style URLs (api-staging.bdsmlr.com/nonnudecuties/following/)
# - subdomain: blogname.bdsmlr.com/page/ style URLs (nonnudecuties.bdsmlr.com/following/)
ROUTING_MODE=path
